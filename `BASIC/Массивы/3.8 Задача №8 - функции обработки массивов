
function checkColor(){
    const array1 = [1, 2, 3];
    console.log(array1.includes(2));
    const pets = ['red', 'green', 'blue'];
    console.log(pets.includes('red'));
    console.log(pets.includes('at'));
}
    checkColor();
